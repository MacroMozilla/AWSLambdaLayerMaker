name: AWS Lambda Layer Build

on:
  push:
    branches:
      - master  # 或改成 main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 拉取代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ 安装依赖到 python/ 文件夹（Layer 格式）
      - name: Install dependencies into python/
        run: |
          echo "📦 Installing dependencies into python/ ..."
          python -m pip install --upgrade pip
          mkdir -p python
          pip install --no-cache-dir -r requirements.txt -t python/
          echo "✅ Dependencies installed in ./python/"

      # 4️⃣ 打包成单一 ZIP（顶层是 python/）
      - name: Create Lambda layer zip
        run: |
          echo "📦 Creating aws_package.zip (top-level: python/)"
          zip -r aws_package.zip python/
          echo "✅ aws_package.zip created successfully!"

      # 5️⃣ 验证 ZIP 内容（确认有 python/）
      - name: Verify ZIP structure
        run: unzip -l aws_package.zip | head -20

      # 6️⃣ 上传 artifact（供下载或部署）
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: aws_layer
          path: aws_package.zip
