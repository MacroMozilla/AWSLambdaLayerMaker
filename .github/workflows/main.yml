name: AWS Package Build

on:
  push:
    branches:
      - master  # 或者改成 main，根据你的仓库分支

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 拉取代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ 安装依赖到 package/ 文件夹
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt -t package/

      # 4️⃣ 复制源码文件进 package/
      - name: Copy source code
        run: |
          cp *.py package/ || true
          # 如果你的源码在 src/ 目录：
          # cp -r src/* package/

      # 5️⃣ 打包成 zip（AWS Lambda 可直接部署）
      - name: Create deployment package
        run: |
          cd package
          zip -r ../aws_package.zip .

      # 6️⃣ 验证 ZIP 内容
      - name: Verify package structure
        run: |
          echo "🔍 Checking ZIP contents..."
          unzip -l aws_package.zip | head -40

      # 7️⃣ 上传构建结果供下载
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: aws_package
          path: aws_package.zip
