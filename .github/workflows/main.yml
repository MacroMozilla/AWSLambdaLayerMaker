name: AWS Lambda Layer Build

on:
  push:
    branches:
      - master  # 或 main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 拉取代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ 安装依赖到 python/ 文件夹
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies into python/..."
          python -m pip install --upgrade pip
          mkdir -p python
          pip install --no-cache-dir -r requirements.txt -t python/
          echo "✅ Dependencies installed into ./python/"
      
      # 6️⃣ 上传 artifact（仅一层 ZIP）
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aws_package
          path: ./
